services:
  app:
    build: .
    ports:
      - "8501:8501"   # Streamlit UI
      - "7860:7860"   # FastAPI (/api + Swagger at /docs)
    env_file:
      - .env
    volumes:
      - ./outputs:/app/outputs
    restart: unless-stopped
